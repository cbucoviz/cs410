<%
final String WEBSITE_TITLE = "Global Events";

// frame width and height in percentage
final float FRAME_HEIGHT = 1.0f;
final float FRAME_WIDTH = 1.0f;

// ticker height in pixels, width in percentage should match the css
final float TICKER_HEIGHT = 20;
final float TICKER_WIDTH = 1.0f;

// search bar height and width in pixels
final float SEARCH_HEIGHT = 20;
final float SEARCH_WIDTH = 200;
%>